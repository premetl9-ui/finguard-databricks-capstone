command:
  - streamlit
  - run
  - app/app.py
  - --server.address=0.0.0.0
  - --server.port=8000
  - --server.headless=true

env:
  - name: ENDPOINT_NAME
    valueFrom: postgres

  - name: FINGUARD_DB_SCHEMA
    value: premetl9